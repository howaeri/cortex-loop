# Graft Records

Add one file per graft:

- `docs/grafts/<name>.md`

Each file should include provenance, license check, adaptation scope, fallback behavior, rollback path, and linked evidence artifacts.
